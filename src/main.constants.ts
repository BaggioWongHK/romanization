import { LangRomanizationMap } from './main.types';

// ISO 639-2
export const LANG_ROMANIZATION_MAP: LangRomanizationMap<string> = {
  'zh-HK': ['jyutping']
};

export const SUPPORTED_FILE_EXT: string[] = ['txt'];
